% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse_route_legs.R
\name{parse_route_points}
\alias{parse_route_points}
\title{Parse the Ordered Stop Sequence of Each Route Leg}
\usage{
parse_route_points(api_response)
}
\arguments{
\item{api_response}{A list containing the parsed JSON
(e.g., loaded from a cached .rds file).}
}
\value{
A tidy tibble with one row per stop, ordered by \code{seq} within each
connection/leg, carrying the stop coordinates, transport mode and line.
}
\description{
Extracts, for every travel leg, the ordered sequence of stops
(boarding stop, intermediate stops, alighting stop) with their coordinates.
Unlike \code{parse_route_legs()}, which returns one row per leg, this returns one
row per stop, allowing routes to be drawn as polylines through real stops.
}
