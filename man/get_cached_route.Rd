% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_requests.R
\name{get_cached_route}
\alias{get_cached_route}
\title{Get Cached Route Data from search.ch API}
\usage{
get_cached_route(from, to, date, time, cache_dir = "cache", num = 5)
}
\arguments{
\item{from}{Character. Origin station ID (e.g., "8507000").}

\item{to}{Character. Destination station ID (e.g., "8507100").}

\item{date}{Character. Date in DD.MM.YYYY format.}

\item{time}{Character. Departure time in HH:MM format.}

\item{cache_dir}{Character. Path to the directory
where cache files are stored.}

\item{num}{Integer. Number of connections to retrieve (default 5).}
}
\value{
A list with the parsed JSON API response.
}
\description{
Wraps \code{get_route()} to provide local caching,
fulfilling the mandatory hackathon requirement.
It checks if an API response is already saved locally as an \code{.rds} file.
If it exists, it loads the local file.
Otherwise, it queries the API, saves the result locally,
and then returns the data.
}
\examples{
\dontrun{
# Assuming a "cache" directory exists in your working directory
route <- get_cached_route(
  from = "8507000",
  to = "8507100",
  date = "25.06.2026",
  time = "08:00",
  cache_dir = "cache"
)
}
}
