% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_requests.R
\name{populate_route_cache}
\alias{populate_route_cache}
\title{Populate Local Cache from Query Table}
\usage{
populate_route_cache(query_table, cache_dir = "cache", num = 5, sleep = 3)
}
\arguments{
\item{query_table}{A data frame or tibble generated
by \code{build_route_query_table()}.}

\item{cache_dir}{Character. Path to the cache directory. Defaults to "cache".}

\item{num}{Integer. Number of connections to retrieve per query (default 5).}

\item{sleep}{Numeric. Seconds to pause between API calls
to avoid rate limits (default 0.5).}
}
\value{
Invisibly returns the original \code{query_table}.
}
\description{
Iterates through a route query table and fetches data for each row using
\code{get_cached_route()}. This function effectively pre-loads your local cache
so that subsequent analysis and mapping steps can run instantly without
hitting the search.ch API. It also handles necessary date reformatting.
}
\examples{
\dontrun{
# Assuming 'my_queries' is your generated query table
my_cache <- file.path(tempdir(), "cache")
populate_route_cache(my_queries, cache_dir = my_cache)
}
}
